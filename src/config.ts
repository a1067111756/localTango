/* 默认配置文件 */
export const defaultGlobalOptions: LocalTangoOptions = {
  // 使用的driver
  driver: 'localStorage',
  // 是否加密
  encrypt: false,
  // 加密key
  encryptKey: 'local_tango_encrypt_key',
  // 存储key默认前缀
  prefixOfKey: '',
  // 全局日志
  logging: false
}

